请分析当前视频滑动窗口。

视频名称：{video_name}
视频类别：{video_category}
已知任务背景：{task_background}
当前窗口：{window_start} 至 {window_end}
采样帧数：{frame_count}
等效采样帧率：{sample_fps:.3f} FPS

输入图像已经严格按照时间先后顺序排列，且不包含 {window_end} 之后的任何画面。

上一窗口压缩状态：
{previous_state_json}

要求：
1. 只分析当前窗口，不总结完整视频，不预测未来。
2. 结合上一窗口状态区分动作的 started、ongoing、ended、instant 或 uncertain。
3. 状态保持不变时，不得重复输出为新的 state_changes。
4. 证据不足时写入 uncertainties，不得用专业常识补全。
5. 严格输出 System Prompt 规定的 JSON 对象。
